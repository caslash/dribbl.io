# PostgreSQL connection string
DATABASE_URL=postgresql://api_user:password@localhost:5432/nba

# Server
PORT=3001

# CORS — comma-separated origins allowed to connect (REST + WebSocket)
CORS_ORIGIN=http://localhost:3000
